// Glass / blur effects have been removed from Comic Center.
// The "Obsidian" design system uses solid elevated surfaces instead.
// This file is retained as a stub so any stale import does not break the build.
//
// Migration: replace AppGlassSpec references with AppElevation + AppColors
// surface tokens (see app_spacing.dart and app_colors.dart).
