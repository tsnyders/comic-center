import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Real widget tests require an initialised Isar instance.
    // Integration tests will be added in Phase 3.
    expect(true, isTrue);
  });
}
